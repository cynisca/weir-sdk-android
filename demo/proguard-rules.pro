# Demo app is not minified (isMinifyEnabled = false); this file exists so the
# release buildType's proguardFiles reference resolves. No app-specific rules
# are needed — the :weir library ships its own consumer rules.
